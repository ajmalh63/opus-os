{
  "name": "opusos-monorepo",
  "private": true,
  "packageManager": "pnpm@10.2.1",
  "scripts": {
    "dev": "pnpm --filter api run dev",
    "test": "pnpm --filter api run test",
    "smoke": "node scripts/smoke-interactions.mjs",
    "e2e": "env SMOKE_CHANNEL=bare node scripts/e2e-personas.mjs",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "wrangler": "^4.125.0"
  }
}